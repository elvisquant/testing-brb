name: "2. 🏗️ Infrastructure Deployment"

on:
  workflow_run:
    workflows: ["1. 🔐 Initial Setup & Secrets Generation"]
    types: [completed]
    branches: [main]

env:
  AWS_REGION: 'us-east-1'

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    outputs:
      secrets_ready: ${{ steps.check.outputs.ready }}
      missing_secrets: ${{ steps.check.outputs.missing }}
    
    steps:
    - name: "🔍 Check if all secrets are set"
      id: check
      run: |
        echo "Checking required secrets..."
        
        REQUIRED_SECRETS=("DB_PASSWORD" "SECRET_KEY" "EC2_SSH_PUBLIC_KEY")
        missing=""
        all_ready="true"
        
        for secret in "${REQUIRED_SECRETS[@]}"; do
          secret_value="${{ secrets[secret] }}"
          if [ -z "$secret_value" ] || [ "$secret_value" = "null" ]; then
            echo "❌ Missing: $secret"
            missing="$missing $secret"
            all_ready="false"
          else
            echo "✅ Found: $secret"
          fi
        done
        
        echo "ready=$all_ready" >> $GITHUB_OUTPUT
        echo "missing_secrets=$missing" >> $GITHUB_OUTPUT

  deploy-infrastructure:
    runs-on: ubuntu-latest
    needs: check-secrets
    if: needs.check-secrets.outputs.secrets_ready == 'true'
    
    steps:
    - name: "📥 Checkout code"
      uses: actions/checkout@v4

    - name: "🔐 Configure AWS credentials"
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: "🏗️ Setup Terraform"
      uses: hashicorp/setup-terraform@v3

    - name: "🪣 Create S3 Bucket if not exists"
      run: |
        # Check if bucket exists, create if it doesn't
        if ! aws s3 ls "s3://brb-app-tf-state-2024" 2>/dev/null; then
          echo "📦 Creating S3 bucket for Terraform state..."
          aws s3 mb s3://brb-app-tf-state-2024 --region ${{ env.AWS_REGION }}
          aws s3api put-bucket-versioning \
            --bucket brb-app-tf-state-2024 \
            --versioning-configuration Status=Enabled
          echo "✅ S3 bucket created"
        else
          echo "✅ S3 bucket already exists"
        fi

    - name: "🔒 Create DynamoDB Table if not exists"
      run: |
        # Check if table exists, create if it doesn't
        if ! aws dynamodb describe-table --table-name brb-app-tf-locks 2>/dev/null; then
          echo "🔐 Creating DynamoDB table for state locking..."
          aws dynamodb create-table \
            --table-name brb-app-tf-locks \
            --attribute-definitions AttributeName=LockID,AttributeType=S \
            --key-schema AttributeName=LockID,KeyType=HASH \
            --billing-mode PAY_PER_REQUEST
          echo "✅ DynamoDB table created"
        else
          echo "✅ DynamoDB table already exists"
        fi

    - name: "⏳ Wait for resources to be ready"
      run: |
        echo "⏳ Waiting for S3 and DynamoDB to be fully ready..."
        sleep 30

    - name: "✅ Initialize Terraform"
      run: |
        cd infrastructure
        terraform init -input=false

    - name: "📊 Plan Infrastructure"
      run: |
        cd infrastructure
        terraform plan -no-color -input=false \
          -var="docker_username=${{ secrets.DOCKER_USERNAME }}" \
          -var="docker_password=${{ secrets.DOCKER_PASSWORD }}" \
          -var="db_password=${{ secrets.DB_PASSWORD }}" \
          -var="secret_key=${{ secrets.SECRET_KEY }}" \
          -var="ssh_public_key=${{ secrets.EC2_SSH_PUBLIC_KEY }}"

    - name: "🚀 Apply Infrastructure"
      run: |
        cd infrastructure
        terraform apply -auto-approve -input=false \
          -var="docker_username=${{ secrets.DOCKER_USERNAME }}" \
          -var="docker_password=${{ secrets.DOCKER_PASSWORD }}" \
          -var="db_password=${{ secrets.DB_PASSWORD }}" \
          -var="secret_key=${{ secrets.SECRET_KEY }}" \
          -var="ssh_public_key=${{ secrets.EC2_SSH_PUBLIC_KEY }}"

    - name: "💾 Get EC2 IP"
      id: get-ip
      run: |
        cd infrastructure
        EC2_IP=$(terraform output -raw ec2_public_ip)
        echo "EC2_IP=$EC2_IP" >> $GITHUB_ENV
        echo "EC2_HOST=$EC2_IP" >> $GITHUB_ENV

    - name: "📤 Save EC2 IP to GitHub Secrets"
      uses: actions/github-script@v7
      env:
        EC2_IP: ${{ env.EC2_IP }}
      with:
        script: |
          const ec2Ip = process.env.EC2_IP;
          if (ec2Ip && ec2Ip !== 'null') {
            try {
              await github.rest.actions.createOrUpdateRepoSecret({
                owner: context.repo.owner,
                repo: context.repo.repo,
                secret_name: 'EC2_IP',
                encrypted_value: ec2Ip
              });
              console.log(`✅ EC2_IP saved to secrets: ${ec2Ip}`);
            } catch (error) {
              console.log('❌ Failed to save EC2_IP:', error.message);
            }
          } else {
            console.log('❌ No EC2 IP found in Terraform outputs');
          }

  secrets-not-ready:
    runs-on: ubuntu-latest
    needs: check-secrets
    if: needs.check-secrets.outputs.secrets_ready == 'false'
    
    steps:
    - name: "❌ Secrets Not Ready"
      run: |
        echo "❌ CANNOT DEPLOY INFRASTRUCTURE - Missing secrets:"
        echo "${{ needs.check-secrets.outputs.missing_secrets }}"
        echo ""
        echo "Please run the '1. 🔐 Initial Setup & Secrets Generation' workflow first."
        echo "It should generate these secrets automatically."
        exit 1

  infrastructure-complete:
    runs-on: ubuntu-latest
    needs: deploy-infrastructure
    if: always()
    
    steps:
    - name: "📋 Infrastructure Status"
      run: |
        echo "🎯 INFRASTRUCTURE DEPLOYMENT COMPLETE"
        echo "===================================="
        if [ "${{ needs.deploy-infrastructure.result }}" == "success" ]; then
          echo "✅ Infrastructure deployed successfully!"
          echo "🌐 Application URL: https://brb.elvisquant.com"
          echo "🚀 Application deployment will now start..."
        else
          echo "❌ Infrastructure deployment failed"
        fi